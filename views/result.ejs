<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proto 1</title>
    <link rel="stylesheet" href="/css/bulma.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container is-max-widescreen">
        <br><br><br><br>
        <div class="block">
            <h1 class="title is-1">Hi! Welcome to our first prototype</h1>
        </div>
        <div class="block">
            <h2 class="subtitle">By <strong>dadylonglegs</strong> and <strong>λman</strong></h2>
        </div>
        <br><br>
        <div class="block box">
            <form action="/" method="POST">
                <div class="field">
                    <label for="textIn" class="label">Text In</label>
                    <div class="control">
                        <% let valIn = textIn; %> 
                      <textarea id="textIn" name="textIn" class="textarea" placeholder="Type your text here"><%= valIn %></textarea>
                    </div>
                </div>
                <div class="control">
                    <button class="button is-link">Submit</button>
                    
                </div>
            </form>
            <form id="clear" action="/" method="GET">
                <button class="button is-link is is-outlined">Clear</button>
            </form>
        </div>
        <div class="block box">
            <div class="field">
                <label class="label">Text Out</label>
                <div class="control">
                    <% let valOut = textOut; %>
                  <textarea id="textOut" class="textarea" placeholder="Your output shows up here"><% for(let sen of valOut ){%>
<%= sen %>
<%}%></textarea>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

</body>
</html>